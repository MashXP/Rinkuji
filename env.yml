name: rinkuji
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - flask       # local dev server (flask run --debug)
  - requests    # used by api/ functions and backend services
  - pytest-cov  # backend test suite